# Cloudflare Worker 프록시 배포 방법 (5분컷)

API 키를 Cloudflare 서버에 숨겨두고, HTML은 프록시 주소만 호출하는 방식이야.
학생/사용자는 키 입력 없이 바로 쓸 수 있어.

---

## 1. Cloudflare 계정 만들기 (이미 있으면 스킵)

1. https://dash.cloudflare.com/sign-up 에서 무료 가입
2. 이메일 인증까지 완료

## 2. Worker 만들기

1. 대시보드 왼쪽 메뉴 → **Workers & Pages** 클릭
2. **Create** 버튼 → **Create Worker** 클릭
3. 이름 정하기 (예: `artist-interview-proxy`) → **Deploy**
4. 배포되면 **Edit code** 클릭
5. 기본 코드 전부 지우고 → 이 폴더의 `worker.js` 내용 복붙
6. 오른쪽 위 **Deploy** 버튼 클릭

## 3. API 키를 비밀 변수로 등록

1. Worker 상세 페이지에서 **Settings** 탭
2. **Variables and Secrets** → **Add**
3. Type: **Secret** 선택
4. Variable name: `ANTHROPIC_API_KEY`
5. Value: 본인 API 키 (sk-ant-...) 붙여넣기
6. **Deploy** 클릭

## 4. 프록시 주소 복사

Worker 상세 페이지 상단에 주소가 있어. 이런 형태:
```
https://artist-interview-proxy.본인계정.workers.dev
```
이걸 복사해둬.

## 5. index.html에 주소 넣기

`index.html` 파일 맨 위쪽에 있는 이 줄을 찾아서 주소를 채워넣어:

```js
const PROXY_URL = "https://여기에_붙여넣기.workers.dev";
```

저장 후 배포하면 끝! 이제 키 입력 화면 없이 바로 예술가 선택 화면으로 들어가져.

---

## 보안 팁 (선택)

- `worker.js`의 `ALLOWED_ORIGINS`를 `["*"]`에서 본인 사이트 주소로 바꾸면
  다른 사람이 프록시 무단 사용하는 걸 막을 수 있어.
  예: `["https://본인사이트.github.io"]`
- Cloudflare 대시보드에서 Worker 호출 횟수도 모니터링할 수 있어.
  비정상 트래픽 보이면 키 재발급하면 돼.
